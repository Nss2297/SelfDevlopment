apiVersion: v1
kind: Service
metadata:
  name: prescription-service-qa-service
  namespace: pbm-qa
  labels:
    app: prescription-service-qa-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: prescription-service-qa-deployment