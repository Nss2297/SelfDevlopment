apiVersion: v1
kind: Service
metadata:
  name: prescription-service-prod-service
  namespace: pbm-prod
  labels:
    app: prescription-service-prod-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: prescription-service-prod-deployment
