apiVersion: v1
kind: Service
metadata:
  name: prescription-service-stg-service
  namespace: pbm-stg
  labels:
    app: prescription-service-stg-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: prescription-service-stg-deployment
