apiVersion: v1
kind: Service
metadata:
  name: payer-customization-service-stg-service
  namespace: pbm-stg
  labels:
    app: payer-customization-service-stg-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: payer-customization-service-stg-deployment
