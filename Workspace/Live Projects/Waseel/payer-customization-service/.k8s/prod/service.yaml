apiVersion: v1
kind: Service
metadata:
  name: payer-customization-service-prod-service
  namespace: pbm-prod
  labels:
    app: payer-customization-service-prod-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: payer-customization-service-prod-deployment
