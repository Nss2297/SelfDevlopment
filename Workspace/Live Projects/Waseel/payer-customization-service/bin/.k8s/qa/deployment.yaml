apiVersion: apps/v1
kind: Deployment
metadata:
  name: payer-customization-service-qa-deployment
  namespace: pbm-qa
  labels:
    app: payer-customization-service-qa-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payer-customization-service-qa-deployment
  template:
    metadata:
      labels:
        app: payer-customization-service-qa-deployment
    spec:
      containers:
        - name: payer-customization-service-qa-container
          image: me-jeddah-1.ocir.io/ax7dvcawpc84/pbm-qa/payer-customization-service:latest
          env:
          - name: TZ
            value: Asia/Riyadh
          ports:
          - containerPort: 8080
      imagePullSecrets:
        - name: waseel-docker-registry-cred
