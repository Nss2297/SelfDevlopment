apiVersion: apps/v1
kind: Deployment
metadata:
  name: payer-customization-service-prod-deployment
  namespace: pbm-prod
  labels:
    app: payer-customization-service-prod-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payer-customization-service-prod-deployment
  template:
    metadata:
      labels:
        app: payer-customization-service-prod-deployment
    spec:
      containers:
        - name: payer-customization-service-prod-container
          image: me-jeddah-1.ocir.io/ax7dvcawpc84/pbm-prod/payer-customization-service:latest
          env:
          - name: TZ
            value: Asia/Riyadh
          ports:
          - containerPort: 8080
      imagePullSecrets:
        - name: waseel-docker-registry-cred
