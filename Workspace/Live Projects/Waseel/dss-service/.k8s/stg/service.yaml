apiVersion: v1
kind: Service
metadata:
  name: dss-service-stg-service
  namespace: pbm-stg
  labels:
    app: dss-service-stg-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: dss-service-stg-deployment