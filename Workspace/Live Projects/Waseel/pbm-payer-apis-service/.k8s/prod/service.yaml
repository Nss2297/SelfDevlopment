apiVersion: v1
kind: Service
metadata:
  name: pbm-payer-apis-service
  namespace: pbm-prod
  labels:
    app: pbm-payer-apis-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: pbm-payer-apis-service
