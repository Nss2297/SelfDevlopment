apiVersion: v1
kind: Service
metadata:
  name: drug-formulary-service
  namespace: pbm-qa
  labels:
    app: drug-formulary-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: drug-formulary-service
