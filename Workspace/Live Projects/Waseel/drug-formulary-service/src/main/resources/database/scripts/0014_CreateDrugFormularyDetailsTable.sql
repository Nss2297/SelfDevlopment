--liquibase formatted sql

--changeset Drug Formulary Service:0014

CREATE TABLE PBM_BUSINESS_RULES.DRUG_FORMULARY_DETAILS
(
	DRUG_FORMULARY_DETAILS_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    FORMULARY_ID NUMBER NOT NULL,
    WASEEL_DRUG_ID NUMBER NOT NULL,
    REGISTRATION_NUMBER VARCHAR2(50) NOT NULL,
    TRADE_NAME VARCHAR2(256) NOT NULL,
    SCIENTIFIC_NAME VARCHAR2(256)NOT NULL,
    SCIENTIFIC_CODE VARCHAR2(150) NOT NULL, 
    PRICE Number(14,2) NOT NULL,  
    IS_OVERRIDE  CHAR(1) DEFAULT ('0') NOT NULL, 
    LAST_UPDATE_DATE DATE NOT NULL,
    IS_DELETED  CHAR(1) DEFAULT ('0') NOT NULL, 
    DELETED_BY VARCHAR2(30),
    CONSTRAINT PK_DRUG_FORMULARY_DETAILS PRIMARY KEY (DRUG_FORMULARY_DETAILS_ID),
    CONSTRAINT FK_DFD_FORMULARY_ID
    	FOREIGN KEY (FORMULARY_ID) REFERENCES PBM_BUSINESS_RULES.DRUG_FORMULARY_METADATA (FORMULARY_ID),
    CONSTRAINT UK_DRUG_FORMULARY_DETAILS UNIQUE (FORMULARY_ID,WASEEL_DRUG_ID,REGISTRATION_NUMBER)
);