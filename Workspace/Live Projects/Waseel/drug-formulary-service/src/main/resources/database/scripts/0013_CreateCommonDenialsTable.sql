--liquibase formatted sql

--changeset Drug Formulary Service:0013

CREATE TABLE PBM_BUSINESS_RULES.COMMON_DENIALS
(
	COMMON_DENIALS_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    DENIAL_CODE VARCHAR2(30) NOT NULL,
    DENIAL_DESCRIPTION VARCHAR2(200) NOT NULL,
    CONSTRAINT PK_COMMON_DENIALS PRIMARY KEY (COMMON_DENIALS_ID),
    CONSTRAINT UK_COMMON_DENIALS UNIQUE (DENIAL_CODE,DENIAL_DESCRIPTION)
);