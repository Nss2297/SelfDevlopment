--liquibase formatted sql

--changeset Drug Formulary Service:0004

CREATE TABLE PBM_BUSINESS_RULES.POLICY_INFORMATION
(
	POLICY_INFORMATION_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    PAYER_ID  VARCHAR2(20) NOT NULL,
    TPA_ID VARCHAR2(20) NOT NULL,
    POLICY_NUMBER VARCHAR2(50) NOT NULL,
    POLICY_HOLDER_NAME   VARCHAR2(250) NOT NULL,
    POLICY_TYPE  VARCHAR2(250) NOT NULL,
    ENDORSEMENT NUMBER NOT NULL,
    ISSUE_DATE DATE NOT NULL,
    START_DATE DATE NOT NULL,
    END_DATE DATE NOT NULL,
    LAST_UPDATE_DATE DATE NOT NULL,
    COVERAGE VARCHAR2(2500),
    EXCLUSION VARCHAR2(2500),
    COMMENTS VARCHAR2(3000),
    CONSTRAINT PK_POLICY_INFORMATION PRIMARY KEY (POLICY_INFORMATION_ID),
    CONSTRAINT UK_POLICY_INFORMATION UNIQUE (PAYER_ID,POLICY_NUMBER)
);