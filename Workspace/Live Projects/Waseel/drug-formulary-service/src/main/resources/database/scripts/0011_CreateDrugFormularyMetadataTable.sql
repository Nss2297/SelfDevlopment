--liquibase formatted sql

--changeset Drug Formulary Service:0011

CREATE TABLE PBM_BUSINESS_RULES.DRUG_FORMULARY_METADATA
(
	FORMULARY_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    PAYER_ID VARCHAR2(20) NOT NULL,
    FORMULARY_NAME VARCHAR2(100) NOT NULL,
    CREATED_DATE DATE NOT NULL,
    CREATED_BY VARCHAR2(30) NOT NULL,
    LAST_UPDATE_DATE DATE NOT NULL,
    IS_DELETED CHAR(1) DEFAULT ('0') NOT NULL,
    DELETED_BY VARCHAR2(30) NOT NULL,
    CONSTRAINT PK_DRUG_FORMULARY_METADATA PRIMARY KEY (FORMULARY_ID),
    CONSTRAINT UK_DRUG_FORMULARY_METADATA UNIQUE (PAYER_ID,FORMULARY_NAME)
);