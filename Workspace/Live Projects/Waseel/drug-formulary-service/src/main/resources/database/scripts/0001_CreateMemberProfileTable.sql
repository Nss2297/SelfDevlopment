--liquibase formatted sql

--changeset Drug Formulary Service:0001

CREATE TABLE PBM_BUSINESS_RULES.MEMBER_PROFILE
(
	MEMBER_PROFILE_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    MEMBER_NAME VARCHAR2(200) NOT NULL,
    ID_NUMBER NUMBER NOT NULL,
    DOB DATE NOT NULL,
    GENDER  VARCHAR2(10) NOT NULL,
    NATIONALITY VARCHAR2(56) NOT NULL,
    MOBILE_NUMBER VARCHAR2(15),
    EMAIL VARCHAR2(64),
    HAS_CHRONIC_DISEASES CHAR(1) DEFAULT('0') NOT NULL,
    LAST_UPDATE_DATE DATE NOT NULL,
    CONSTRAINT PK_MEMBER_PROFILE PRIMARY KEY (MEMBER_PROFILE_ID)
);