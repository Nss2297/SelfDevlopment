--liquibase formatted sql

--changeset Drug Formulary Service:0008

CREATE TABLE PBM_BUSINESS_RULES.BENEFIT_SUBCOVERAGE
(
	BENEFIT_SUBCOVERAGE_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    CLASS_BENEFIT_ID NUMBER NOT NULL,
    SUBCOVERAGE_CODE VARCHAR2(30) NOT NULL,
    SUBCOVERAGE_DESCRIPTION VARCHAR2(250) NOT NULL,
    SUBCOVERAGE_LIMIT_VALUE NUMBER,
    SUBCOVERAGE_LIMIT_CURRENCY VARCHAR2(30),
    SUBCOVERAGE_APP_THRESHOLD_VALUE NUMBER,
    SUBCOVERAGE_APPROVAL_THRESHOLD_CURRENCY VARCHAR2(30),
    CONSTRAINT PK_BENEFIT_SUBCOVERAGE PRIMARY KEY (BENEFIT_SUBCOVERAGE_ID),
    CONSTRAINT FK_BENEFIT_SUBCOVERAGE_CLASS_BENEFIT_ID
    	FOREIGN KEY (CLASS_BENEFIT_ID) REFERENCES PBM_BUSINESS_RULES.CLASS_BENEFITS (CLASS_BENEFIT_ID),
    CONSTRAINT UK_BENEFIT_SUBCOVERAGE UNIQUE (CLASS_BENEFIT_ID,SUBCOVERAGE_CODE)
);