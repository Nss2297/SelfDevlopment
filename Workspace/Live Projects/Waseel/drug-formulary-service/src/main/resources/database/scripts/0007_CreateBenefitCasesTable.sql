--liquibase formatted sql

--changeset Drug Formulary Service:0007

CREATE TABLE PBM_BUSINESS_RULES.BENEFIT_CASES
(
	BENEFIT_CASE_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    CLASS_BENEFIT_ID NUMBER NOT NULL,
    CASE_CODE VARCHAR2(30) NOT NULL,
    PATIENT_SHARE_VALUE NUMBER NOT NULL,
    PATIENT_SHARE_CURRENCY VARCHAR2(30) NOT NULL,
    MAX_PATIENT_SHARE_VALUE NUMBER NOT NULL,
    MAX_PATIENT_SHARE_CURRENCY VARCHAR2(30) NOT NULL,
    MAX_CONSULTATION_FEE_VALUE NUMBER NOT NULL,
    MAX_CONSULTATION_FEE_CURRENCY VARCHAR2(30) NOT NULL,
    APPROVAL_THRESHOLD_VALUE NUMBER NOT NULL,
    APPROVAL_THRESHOLD_CURRENCY	VARCHAR2(30) NOT NULL,
    CONSTRAINT PK_BENEFIT_CASES PRIMARY KEY (BENEFIT_CASE_ID),
    CONSTRAINT FK_BENEFIT_CASES_CLASS_BENEFIT_ID
    	FOREIGN KEY (CLASS_BENEFIT_ID) REFERENCES PBM_BUSINESS_RULES.CLASS_BENEFITS (CLASS_BENEFIT_ID),
    CONSTRAINT UK_BENEFIT_CASES UNIQUE (CLASS_BENEFIT_ID,CASE_CODE)
);