--liquibase formatted sql

--changeset PBMNotification:0002

CREATE TABLE PBM_BUSINESS_RULES.EMAIL_SMS_AUDITLOG
(
	EMAIL_SMS_AUDITLOG_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    MOBILE_NUMBER  VARCHAR2(15),
    EMAIL  VARCHAR2(100),
    REQUEST_TYPE  VARCHAR2(30) NOT NULL,
	STATUS VARCHAR2(60),
	STATUS_DESCRIPTION VARCHAR2(1500),
	TRANSACTION_STATUS VARCHAR2(10) NOT NULL,
	RECEIVING_RESPONSE_DATE_TIME  TIMESTAMP(6),
	SENDING_REQUEST_DATE_TIME TIMESTAMP(6) NOT NULL,
	REQUEST_ID VARCHAR2(100) NOT NULL,
	EPRESCRIPTION_REFERENCE_NUMBER VARCHAR2(100) NOT NULL,
	UNIFONIC_MESSAGE_ID VARCHAR2(15),
	UNIFONIC_TIME_CREATED VARCHAR2(15),
	PATIENT_URL VARCHAR2(2000) NOT NULL,
    CONSTRAINT PK_EMAIL_SMS_AUDITLOG_ID PRIMARY KEY (EMAIL_SMS_AUDITLOG_ID)
);