--liquibase formatted sql

--changeset PBMNotification:0001

CREATE TABLE PBM_BUSINESS_RULES.PAYER_API_KEY_INFORMATION
(
	PAYER_API_KEY_INFORMATION_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    PAYER_ID  VARCHAR2(20) NOT NULL,
    PAYER_NAME  VARCHAR2(100) NOT NULL,
    STANDARD_TRANSACTION_ID  NUMBER(10,6) NOT NULL ,
    STANDARD_TRANSACTION_NAME VARCHAR2(80) NOT NULL,
    API_KEY VARCHAR2(3000),
    CONSTRAINT PK_PAKI_ID PRIMARY KEY (PAYER_API_KEY_INFORMATION_ID)
);