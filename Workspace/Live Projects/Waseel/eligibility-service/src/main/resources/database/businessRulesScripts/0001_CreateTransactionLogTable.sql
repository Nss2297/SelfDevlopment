--liquibase formatted sql

--changeset Eligibility Service:0001

CREATE TABLE "PBM_BUSINESS_RULES".TRANSACTION_LOG 
(
  TRANSACTION_LOG_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL
, TRANSACTION_ID NUMBER NOT NULL 
, REQUEST_ID VARCHAR2(100) 
, TRANSACTION_TYPE VARCHAR2(100) NOT NULL 
, PAYER_ID VARCHAR2(20) 
, TRANSACTION_REFERENCE_NUMBER VARCHAR2(20) 
, PROVIDER_ID VARCHAR2(20) 
, STATUS VARCHAR2(60) 
, STATUS_DESCRIPTION VARCHAR2(500) 
, RECEIVING_REQUEST_DATE_TIME TIMESTAMP NOT NULL 
, SENDING_RESPONSE_DATE_TIME TIMESTAMP NOT NULL 
, HTTP_STATUS VARCHAR2(30) 
, HTTP_STATUS_DESCRIPTION VARCHAR2(5000) 
, CONSTRAINT PBM_BUSINESS_RULES_PK PRIMARY KEY(TRANSACTION_LOG_ID)
);
