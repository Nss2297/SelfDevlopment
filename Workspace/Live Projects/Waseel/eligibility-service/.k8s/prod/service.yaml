apiVersion: v1
kind: Service
metadata:
  name: eligibility-service-prod-service
  namespace: pbm-prod
  labels:
    app: eligibility-service-prod-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: eligibility-service-prod-deployment
