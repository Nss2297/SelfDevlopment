apiVersion: v1
kind: Service
metadata:
  name: eligibility-service-stg-service
  namespace: pbm-stg
  labels:
    app: eligibility-service-stg-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: eligibility-service-stg-deployment
