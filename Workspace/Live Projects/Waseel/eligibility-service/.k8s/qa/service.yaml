apiVersion: v1
kind: Service
metadata:
  name: eligibility-service-qa-service
  namespace: pbm-qa
  labels:
    app: eligibility-service-qa-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: eligibility-service-qa-deployment
