--liquibase formatted sql

--changeset Drug Exclusion Validation Service:0001

CREATE TABLE PBM_BUSINESS_RULES.DRUG_EXCLUSION_METADATA
(
	EXCLUSION_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    EXCLUSION_NAME VARCHAR2(100) NOT NULL,
    CREATED_DATE DATE NOT NULL,
    CREATED_BY VARCHAR2(30) NOT NULL,
    LAST_UPDATE_DATE DATE NOT NULL,
    IS_DELETED CHAR(1) DEFAULT ('0') NOT NULL,
    DELETED_BY VARCHAR2(30) NOT NULL,
    CONSTRAINT PK_DEM_EXCLUSION_ID PRIMARY KEY (EXCLUSION_ID),
    CONSTRAINT UK_DEM_EXCLUSION_NAME UNIQUE (EXCLUSION_NAME)
);