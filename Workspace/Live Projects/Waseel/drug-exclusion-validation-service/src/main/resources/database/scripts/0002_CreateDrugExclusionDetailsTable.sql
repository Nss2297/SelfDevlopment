--liquibase formatted sql

--changeset Drug Exclusion Validation Service:0002

CREATE TABLE PBM_BUSINESS_RULES.DRUG_EXCLUSION_DETAILS
(
	DRUG_EXCLUSION_DETAILS_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    EXCLUSION_ID NUMBER NOT NULL,
    WASEEL_DRUG_ID NUMBER NOT NULL,
    REGISTRATION_NUMBER VARCHAR2(50) NOT NULL,
    TRADE_NAME VARCHAR2(256) NOT NULL,
    SCIENTIFIC_NAME VARCHAR2(256)NOT NULL,
    SCIENTIFIC_CODE VARCHAR2(150) NOT NULL,
    PRICE NUMBER(14,2) NOT NULL,
    LAST_UPDATE_DATE DATE NOT NULL,
    IS_DELETED  CHAR(1) DEFAULT ('0') NOT NULL,
    DELETED_BY VARCHAR2(30),
    CONSTRAINT PK_DED_DRUG_EXCLUSION_DETAILS_ID PRIMARY KEY (DRUG_EXCLUSION_DETAILS_ID),
    CONSTRAINT FK_DED_EXCLUSION_ID
        FOREIGN KEY (EXCLUSION_ID) REFERENCES PBM_BUSINESS_RULES.DRUG_EXCLUSION_METADATA (EXCLUSION_ID),
    CONSTRAINT UK_DED_EID_WDID_RN UNIQUE (EXCLUSION_ID,WASEEL_DRUG_ID,REGISTRATION_NUMBER)
);