--liquibase formatted sql

--changeset Drug Exclusion Validation Service:0006

CREATE TABLE PBM_BUSINESS_RULES.DEPT_SPEC_PHYSC_ASSC
(
	DEPT_SPEC_PHYSC_ASSC_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
	DEPARTMENT_ID NUMBER NOT NULL,
	SPECIALITY_ID  NUMBER NOT NULL,
	"PhysicianInfoId" NUMBER NOT NULL,
	IS_ENABLED CHAR(1) DEFAULT ('0') NOT NULL,
	CONSTRAINT PK_DSPA_DSPASSC_ID PRIMARY KEY (SPECIALITY_ID),
	CONSTRAINT UK_DEPT_SPEC_PHYSC_ASSC UNIQUE (DEPARTMENT_ID,SPECIALITY_ID,"PhysicianInfoId"),
	CONSTRAINT FK_DSPA_SPECIALITY_ID
    	FOREIGN KEY (SPECIALITY_ID) REFERENCES PBM_BUSINESS_RULES.SPECIALITY (SPECIALITY_ID),
    CONSTRAINT FK_DSPA_DEPARTMENT_ID
    	FOREIGN KEY (DEPARTMENT_ID) REFERENCES PBM_BUSINESS_RULES.DEPARTMENT (DEPARTMENT_ID),
    CONSTRAINT "FK_DSPA_PhysicianInfoId"
    	FOREIGN KEY ("PhysicianInfoId") REFERENCES PBM_BUSINESS_RULES."PhysicianInfo" ("PhysicianInfoId")
);
