--liquibase formatted sql

--changeset Drug Exclusion Validation Service:0010

CREATE TABLE PROVIDER_EXCLUSION_ASSC 
(
  PROVIDER_EXCLUSION_ASSC_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL
, PROVIDER_ID NUMBER NOT NULL 
, EXCLUSION_ID NUMBER NOT NULL 
, PROVIDER_NAME VARCHAR2(100) NOT NULL 
, IS_ENABLED CHAR(1) DEFAULT ('1') NOT NULL
, LAST_UPDATE_DATE DATE NOT NULL 
, CONSTRAINT PROVIDER_EXCLUSION_ASSC_PK PRIMARY KEY (PROVIDER_EXCLUSION_ASSC_ID)
, CONSTRAINT UK_PROVIDER_EXCLUSION_ASSC UNIQUE (PROVIDER_ID,EXCLUSION_ID)
, CONSTRAINT FK_PEA_EXCLUSION_ID FOREIGN KEY (EXCLUSION_ID) REFERENCES PBM_BUSINESS_RULES.DRUG_EXCLUSION_METADATA (EXCLUSION_ID)
);