--liquibase formatted sql

--changeset Prescription:0008

CREATE TABLE "PBM_BUSINESS_RULES"."PROVIDER_NETWORK_ASSC"
(
	"PROVIDER_NETWORK_ASSC_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
	"NETWORK_ID" NUMBER NOT NULL,
	"PROVIDER_ID" NUMBER NOT NULL,
    "IS_ENABLED" CHAR(1) DEFAULT '1',
    "LAST_UPDATE_DATE" DATE NOT NULL,
	CONSTRAINT PK_NetworkID_Assc PRIMARY KEY ("PROVIDER_NETWORK_ASSC_ID"),
	CONSTRAINT FK_Network_Id FOREIGN KEY ("NETWORK_ID") REFERENCES "PBM_BUSINESS_RULES"."PROVIDER_NETWORK" ("NETWORK_ID")
);