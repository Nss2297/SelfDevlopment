apiVersion: v1
kind: Service
metadata:
  name: pbm-gateway-service-prod-service
  namespace: pbm-prod
  labels:
    app: pbm-gateway-service-prod-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: pbm-gateway-service-prod-deployment
