apiVersion: v1
kind: Service
metadata:
  name: pbm-gateway-service-stg-service
  namespace: pbm-stg
  labels:
    app: pbm-gateway-service-stg-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: pbm-gateway-service-stg-deployment
