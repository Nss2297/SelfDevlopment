apiVersion: v1
kind: Service
metadata:
  name: waseel-sms-service
  namespace: pbm-stg
  labels:
    app: waseel-sms-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: waseel-sms-service
