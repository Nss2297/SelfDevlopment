apiVersion: v1
kind: Service
metadata:
  name: rts-service-prod-service
  namespace: pbm-prod
  labels:
    app: rts-service-prod-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: rts-service-prod-deployment
