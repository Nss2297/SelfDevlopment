apiVersion: apps/v1
kind: Deployment
metadata:
  name: rts-service-prod-deployment
  namespace: pbm-prod
  labels:
    app: rts-service-prod-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: rts-service-prod-deployment
  template:
    metadata:
      labels:
        app: rts-service-prod-deployment
    spec:
      containers:
        - name: rts-service-prod-container
          image: me-jeddah-1.ocir.io/ax7dvcawpc84/pbm-prod/rts-service:latest
          env:
          - name: TZ
            value: Asia/Riyadh
          ports:
          - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
              scheme: HTTP
            timeoutSeconds: 10
            periodSeconds: 10
            successThreshold: 1
            failureThreshold: 20
      imagePullSecrets:
        - name: waseel-docker-registry-cred
