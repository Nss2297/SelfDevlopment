--liquibase formatted sql

--changeset PBM Scheduler Service:0001

CREATE TABLE PBM_BUSINESS_RULES.SCHEDULER
(
	SCHEDULER_ID NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1) NOT NULL,
    PROCESS_TYPE VARCHAR2(30),
    INTERVAL VARCHAR2(50),
    CONSTRAINT PK_SCHEDULER_ID PRIMARY KEY (SCHEDULER_ID)
);