apiVersion: v1
kind: Service
metadata:
  name: dss-admin-service
  namespace: pbm-prod
  labels:
    app: dss-admin-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: dss-admin-service
