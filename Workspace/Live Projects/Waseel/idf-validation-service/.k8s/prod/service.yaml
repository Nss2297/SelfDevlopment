apiVersion: v1
kind: Service
metadata:
  name: idf-validation-service-prod-service
  namespace: pbm-prod
  labels:
    app: idf-validation-service-prod-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: idf-validation-service-prod-deployment
