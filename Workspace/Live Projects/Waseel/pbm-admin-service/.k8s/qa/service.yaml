apiVersion: v1
kind: Service
metadata:
  name: admin-service-qa-service
  namespace: pbm-qa
  labels:
    app: admin-service-qa-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: admin-service-qa-deployment
