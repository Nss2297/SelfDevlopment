apiVersion: v1
kind: Service
metadata:
  name: admin-service-stg-service
  namespace: pbm-stg
  labels:
    app: admin-service-stg-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: admin-service-stg-deployment
